'use strict';

const rwTest = require('../lib/read-write');
const fs = require('fs');

describe('Testing read-write.js', function() {
  beforeAll(done => {
    done();
  });
  describe('default properties', () => {
    test('', done => {
      done();
    });
  });
});
